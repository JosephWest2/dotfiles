---
description: Work in read-only mode without editing the repository
argument-hint: "[request]"
---
Handle the following request without editing the repository:

${ARGUMENTS:-Use the request already described in the conversation. If there is no clear request, ask me what I need.}
